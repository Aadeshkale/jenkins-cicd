pipeline{
    agent any

    stages{
        stage("System ENVS") {
            step{
            sh """
                echo ${env. branch}
                echo ${printenv | sort }
            """

            }
        }
    }   





}