pipeline{
    agent any

    stages{ 

        stage("System-ENVS") {
            steps{
                sh 'printenv | sort'
            }
        }

        stage('Build Image'){
            steps{
                script{
                    if (env.GGIT_BRANCH == 'main'){
                        sh "echo This is main Branch"
                    }
                }
            }

        }


    }   
}